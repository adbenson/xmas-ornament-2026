import type { FC } from "react"
import { PixelPalette } from "../emulator/PixelPalette"

export const Emulator: FC = () => {
    return (
        <div>
            <PixelPalette />
        </div>
    )
}